---
phase: 06-direct-websocket-transport
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramPoller.kt
  - shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramApi.kt
  - shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepository.kt
  - shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepositoryImpl.kt
  - shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/dto/TelegramResponse.kt
  - shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/dto/TelegramUpdate.kt
  - androidApp/src/main/java/dev/heyduk/relay/service/PollingService.kt
  - androidApp/src/main/java/dev/heyduk/relay/data/DataStoreOffsetProvider.kt
autonomous: false
requirements: [R-06-06, R-06-07]

must_haves:
  truths:
    - "All Telegram transport code is deleted from the codebase"
    - "App builds successfully with only WebSocket transport"
    - "End-to-end message flow works: hook -> relay-server -> WebSocket -> app"
  artifacts:
    - path: "shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/WebSocketClient.kt"
      provides: "WebSocket client (from Plan 02, verified working)"
    - path: "androidApp/src/main/java/dev/heyduk/relay/service/WebSocketService.kt"
      provides: "WebSocket foreground service (from Plan 03, verified working)"
  key_links:
    - from: "zellij-claude/hooks/relay-server.cjs"
      to: "WebSocketClient.kt"
      via: "WebSocket connection on port 9784"
      pattern: "ws://.*:9784"
    - from: "hooks/permission-notify.cjs"
      to: "hooks/telegram-helper.cjs sendRelay()"
      via: "async sendRelay -> Unix socket -> relay-server -> WebSocket -> app"
      pattern: "sendRelay"
---

<objective>
Delete all Telegram transport code and verify the full build compiles. Run integration checkpoint.

Purpose: Complete the hard cutover by removing dead Telegram transport code and verifying the app builds cleanly with only WebSocket transport.

Output: Clean codebase with no Telegram transport code. Build verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-direct-websocket-transport/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Telegram transport code and fix remaining references</name>
  <files>
    shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramPoller.kt
    shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramApi.kt
    shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepository.kt
    shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepositoryImpl.kt
    shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/dto/TelegramResponse.kt
    shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/dto/TelegramUpdate.kt
    androidApp/src/main/java/dev/heyduk/relay/service/PollingService.kt
    androidApp/src/main/java/dev/heyduk/relay/data/DataStoreOffsetProvider.kt
    shared/src/commonTest/kotlin/dev/heyduk/relay/data/remote/TelegramPollerTest.kt
    shared/src/commonTest/kotlin/dev/heyduk/relay/data/remote/TelegramApiTest.kt
  </files>
  <read_first>
    /Users/jheyduk/prj/relay/shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramPoller.kt
    /Users/jheyduk/prj/relay/shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramApi.kt
    /Users/jheyduk/prj/relay/shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepository.kt
    /Users/jheyduk/prj/relay/shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepositoryImpl.kt
    /Users/jheyduk/prj/relay/androidApp/src/main/java/dev/heyduk/relay/service/PollingService.kt
    /Users/jheyduk/prj/relay/androidApp/src/main/java/dev/heyduk/relay/data/DataStoreOffsetProvider.kt
  </read_first>
  <action>
    Delete the following files entirely (they are dead code after Plans 02 and 03):

    **Shared module — transport layer:**
    - `shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramPoller.kt` (replaced by WebSocketClient)
    - `shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramApi.kt` (no longer needed — app doesn't talk to Telegram)
    - `shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/dto/TelegramResponse.kt` (Telegram API response wrapper)
    - `shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/dto/TelegramUpdate.kt` (Telegram update DTO)

    **Shared module — repository layer:**
    - `shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepository.kt` (replaced by RelayRepository)
    - `shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepositoryImpl.kt` (replaced by RelayRepositoryImpl)

    **Android module:**
    - `androidApp/src/main/java/dev/heyduk/relay/service/PollingService.kt` (replaced by WebSocketService)
    - `androidApp/src/main/java/dev/heyduk/relay/data/DataStoreOffsetProvider.kt` (no polling offset needed)

    **Tests:**
    - `shared/src/commonTest/kotlin/dev/heyduk/relay/data/remote/TelegramPollerTest.kt` (dead test)
    - `shared/src/commonTest/kotlin/dev/heyduk/relay/data/remote/TelegramApiTest.kt` (dead test)

    After deletion, search the entire codebase for any remaining references to deleted types:
    - `grep -r "TelegramPoller\|TelegramApi\|TelegramRepository\|TelegramRepositoryImpl\|PollingService\|DataStoreOffsetProvider\|OffsetProvider" --include="*.kt" shared/ androidApp/`
    - Fix any remaining import statements or references found

    Specifically check:
    - `shared/src/commonMain/kotlin/dev/heyduk/relay/di/SharedModule.kt` — should have no Telegram imports
    - `androidApp/src/main/java/dev/heyduk/relay/di/AndroidModule.kt` — should have no Telegram imports
    - `androidApp/src/main/java/dev/heyduk/relay/util/PollingServiceLauncher.kt` — should reference WebSocketService only
    - Any ViewModel files that might still import TelegramRepository
  </action>
  <verify>
    <automated>cd /Users/jheyduk/prj/relay && test ! -f shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramPoller.kt && test ! -f shared/src/commonMain/kotlin/dev/heyduk/relay/data/remote/TelegramApi.kt && test ! -f shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/TelegramRepository.kt && test ! -f androidApp/src/main/java/dev/heyduk/relay/service/PollingService.kt && echo "All Telegram files deleted"</automated>
  </verify>
  <acceptance_criteria>
    - TelegramPoller.kt does not exist
    - TelegramApi.kt does not exist
    - TelegramRepository.kt does not exist
    - TelegramRepositoryImpl.kt does not exist
    - TelegramResponse.kt does not exist
    - TelegramUpdate.kt does not exist
    - PollingService.kt does not exist
    - DataStoreOffsetProvider.kt does not exist
    - TelegramPollerTest.kt does not exist
    - TelegramApiTest.kt does not exist
    - `grep -r "TelegramPoller\|TelegramApi\b\|TelegramRepository\|PollingService\|DataStoreOffsetProvider" --include="*.kt" shared/src/commonMain/ androidApp/src/main/` returns zero results
  </acceptance_criteria>
  <done>All Telegram transport code deleted. No dangling references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full build compiles</name>
  <files></files>
  <read_first>
    /Users/jheyduk/prj/relay/shared/build.gradle.kts
    /Users/jheyduk/prj/relay/androidApp/build.gradle.kts
  </read_first>
  <action>
    Run the Gradle build to verify everything compiles:

    ```bash
    cd /Users/jheyduk/prj/relay && ./gradlew :shared:compileKotlinJvm :androidApp:compileDebugKotlin 2>&1
    ```

    If compilation fails:
    1. Read the error messages carefully
    2. Fix import statements, missing types, or incorrect references
    3. Common issues to watch for:
       - Stale imports of deleted Telegram types
       - ViewModels still expecting TelegramRepository in constructor
       - SharedModule still referencing removed types
       - PollingServiceLauncher referencing PollingService
    4. Re-run build until it passes

    Also run existing tests to check for regressions:
    ```bash
    cd /Users/jheyduk/prj/relay && ./gradlew :shared:jvmTest 2>&1
    ```

    Fix any test failures caused by the refactoring (e.g., tests importing deleted types).
  </action>
  <verify>
    <automated>cd /Users/jheyduk/prj/relay && ./gradlew :shared:compileKotlinJvm :androidApp:compileDebugKotlin --quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :shared:compileKotlinJvm` exits with code 0
    - `./gradlew :androidApp:compileDebugKotlin` exits with code 0
    - `./gradlew :shared:jvmTest` exits with code 0 (existing tests pass)
  </acceptance_criteria>
  <done>Full project compiles with WebSocket transport only. No Telegram transport code remains. Existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end integration verification</name>
  <files></files>
  <action>Human verifies the complete WebSocket transport flow end-to-end. Mac-side relay-server.cjs with mDNS advertisement, app-side WebSocketClient with NSD discovery, all Telegram code removed. Follow the verification steps below.</action>
  <what-built>Complete WebSocket transport replacing Telegram. Mac-side relay-server.cjs with mDNS, app-side WebSocketClient with NSD discovery, all Telegram code removed.</what-built>
  <how-to-verify>
    1. On Mac: ensure ~/.config/zellij-claude/relay.json exists with port and secret fields
    2. On Mac: start a zellij-claude session — relay-server should auto-start (check `ps aux | grep relay-server`)
    3. On Mac: verify mDNS: `dns-sd -B _relay._tcp local` should show the Relay service
    4. On Android: open Relay app — setup screen should ask for "Server Secret" and "WireGuard IP (optional)"
    5. Enter the secret from relay.json, save
    6. App should show "Relay connected" in the foreground notification
    7. Trigger a permission request in a Claude Code session — should appear in the app within 1 second
    8. Send a message from the app — should arrive at the session
  </how-to-verify>
  <verify>Human confirms steps 1-8 pass</verify>
  <done>End-to-end WebSocket transport verified: messages flow from hooks through relay-server over WebSocket to the Android app, and commands flow back from app to sessions</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Project builds: `./gradlew :shared:compileKotlinJvm :androidApp:compileDebugKotlin` succeeds
- Tests pass: `./gradlew :shared:jvmTest` succeeds
- No Telegram code: `grep -r "TelegramPoller\|TelegramApi\b\|TelegramRepository\|PollingService" --include="*.kt" shared/src/commonMain/ androidApp/src/main/` returns empty
- relay-server.cjs exists in zellij-claude hooks
- Human verifies end-to-end WebSocket flow
</verification>

<success_criteria>
- All Telegram transport files deleted (10 files)
- No compilation errors
- Existing tests pass
- Human confirms end-to-end WebSocket flow works (hook -> server -> app -> response)
</success_criteria>

<output>
After completion, create `.planning/phases/06-direct-websocket-transport/06-04-SUMMARY.md`
</output>
