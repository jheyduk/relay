---
phase: 02-session-discovery-status
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - androidApp/src/main/java/dev/heyduk/relay/di/AndroidModule.kt
  - androidApp/src/main/java/dev/heyduk/relay/MainActivity.kt
  - androidApp/src/main/java/dev/heyduk/relay/presentation/navigation/RelayNavGraph.kt
  - shared/src/commonMain/kotlin/dev/heyduk/relay/di/SharedModule.kt
autonomous: false
requirements: [SESS-01, SESS-02, SESS-03, SESS-04, SESS-05, UI-01]

must_haves:
  truths:
    - "App starts with RelayTheme wrapping all content (dark mode works)"
    - "Navigation routes to session list screen when configured (instead of old status screen)"
    - "SessionListViewModel is injected via Koin with all dependencies resolved"
    - "SessionRepository is registered as singleton in Koin and wired to TelegramRepository"
    - "Debug APK assembles and installs successfully"
    - "User can see session list, pull to refresh, use drawer, send commands"
  artifacts:
    - path: "androidApp/src/main/java/dev/heyduk/relay/di/AndroidModule.kt"
      provides: "Koin registrations for SessionListViewModel and SessionRepository"
    - path: "androidApp/src/main/java/dev/heyduk/relay/MainActivity.kt"
      provides: "RelayTheme wrapping all content"
    - path: "androidApp/src/main/java/dev/heyduk/relay/presentation/navigation/RelayNavGraph.kt"
      provides: "Routes to SessionListScreen instead of StatusScreen"
    - path: "shared/src/commonMain/kotlin/dev/heyduk/relay/di/SharedModule.kt"
      provides: "SessionRepository singleton registration"
  key_links:
    - from: "MainActivity"
      to: "RelayTheme"
      via: "Wraps setContent in RelayTheme composable"
      pattern: "RelayTheme"
    - from: "RelayNavGraph"
      to: "SessionListScreen"
      via: "composable(\"sessions\") route"
      pattern: "SessionListScreen"
    - from: "AndroidModule"
      to: "SessionListViewModel"
      via: "viewModel { SessionListViewModel(get(), get(), get()) }"
      pattern: "SessionListViewModel"
    - from: "SharedModule"
      to: "SessionRepositoryImpl"
      via: "single<SessionRepository> { SessionRepositoryImpl(get()) }"
      pattern: "SessionRepository"
---

<objective>
Wire all Phase 2 components together: register SessionRepository in Koin SharedModule, register SessionListViewModel in AndroidModule, update MainActivity to use RelayTheme, update navigation to route to SessionListScreen, and verify the debug APK assembles.

Purpose: Connect the domain layer (Plan 01) and UI layer (Plan 02) into a working application. Without this wiring, the new UI components exist but are unreachable.
Output: 4 modified files, working debug APK with session list as the main screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-discovery-status/02-RESEARCH.md
@.planning/phases/02-session-discovery-status/02-01-SUMMARY.md
@.planning/phases/02-session-discovery-status/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and 02 outputs -->

From shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/SessionRepository.kt:
```kotlin
interface SessionRepository {
    val sessions: Flow<List<Session>>
    val selectedKuerzel: Flow<String?>
    suspend fun refreshSessions()
    fun selectSession(kuerzel: String?)
    suspend fun getLastResponse(kuerzel: String): String?
}
```

From shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/SessionRepositoryImpl.kt:
```kotlin
class SessionRepositoryImpl(
    private val telegramRepository: TelegramRepository,
    scope: CoroutineScope = CoroutineScope(Dispatchers.Default)
) : SessionRepository
```

From androidApp SessionListViewModel (created in Plan 02):
```kotlin
class SessionListViewModel(
    sessionRepository: SessionRepository,
    telegramRepository: TelegramRepository,
    networkMonitor: NetworkMonitor
) : ViewModel()
```

From androidApp SessionListScreen (created in Plan 02):
```kotlin
@Composable
fun SessionListScreen(
    onNavigateToSetup: () -> Unit,
    viewModel: SessionListViewModel = koinViewModel()
)
```

From androidApp RelayTheme (created in Plan 02):
```kotlin
@Composable
fun RelayTheme(
    darkTheme: Boolean = isSystemInDarkTheme(),
    dynamicColor: Boolean = true,
    content: @Composable () -> Unit
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register SessionRepository in SharedModule and SessionListViewModel in AndroidModule</name>
  <files>
    shared/src/commonMain/kotlin/dev/heyduk/relay/di/SharedModule.kt,
    androidApp/src/main/java/dev/heyduk/relay/di/AndroidModule.kt
  </files>
  <read_first>
    shared/src/commonMain/kotlin/dev/heyduk/relay/di/SharedModule.kt,
    androidApp/src/main/java/dev/heyduk/relay/di/AndroidModule.kt,
    shared/src/commonMain/kotlin/dev/heyduk/relay/data/repository/SessionRepositoryImpl.kt,
    androidApp/src/main/java/dev/heyduk/relay/presentation/session/SessionListViewModel.kt
  </read_first>
  <action>
    1. Modify `SharedModule.kt`:
    - Add import for `SessionRepository`, `SessionRepositoryImpl`
    - Add `single<SessionRepository> { SessionRepositoryImpl(get()) }` to the sharedModule
    - The `get()` resolves `TelegramRepository` which is already registered in SharedModule
    - Note: SharedModule may currently register TelegramRepository conditionally or with specific qualifiers. Read the file first and place the SessionRepository registration after TelegramRepository so Koin can resolve the dependency.

    2. Modify `AndroidModule.kt`:
    - Add import for `SessionListViewModel`
    - Add `viewModel { SessionListViewModel(get(), get(), get()) }` to the androidModule
    - The three `get()` calls resolve: SessionRepository (from SharedModule), TelegramRepository (from SharedModule), NetworkMonitor (from AndroidModule)
    - Keep existing SetupViewModel and StatusViewModel registrations (StatusScreen is still needed for backward compat or can be kept as fallback)
  </action>
  <verify>
    <automated>cd /Users/jheyduk/prj/relay && ./gradlew :androidApp:compileDebugKotlin -x lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SharedModule registers SessionRepository as singleton
    - AndroidModule registers SessionListViewModel with correct 3-parameter constructor
    - Compilation succeeds without Koin resolution errors at compile time
  </acceptance_criteria>
  <done>DI modules updated, compilation passes</done>
</task>

<task type="auto">
  <name>Task 2: Update MainActivity with RelayTheme and RelayNavGraph with session list route</name>
  <files>
    androidApp/src/main/java/dev/heyduk/relay/MainActivity.kt,
    androidApp/src/main/java/dev/heyduk/relay/presentation/navigation/RelayNavGraph.kt
  </files>
  <read_first>
    androidApp/src/main/java/dev/heyduk/relay/MainActivity.kt,
    androidApp/src/main/java/dev/heyduk/relay/presentation/navigation/RelayNavGraph.kt,
    androidApp/src/main/java/dev/heyduk/relay/presentation/session/SessionListScreen.kt,
    androidApp/src/main/java/dev/heyduk/relay/presentation/theme/RelayTheme.kt
  </read_first>
  <action>
    1. Modify `MainActivity.kt`:
    - Replace `MaterialTheme { ... }` with `RelayTheme { ... }` in setContent
    - Add import for `dev.heyduk.relay.presentation.theme.RelayTheme`
    - Keep everything else the same (DataStore config check, navController, etc.)
    - This single change enables dark mode + dynamic color for the entire app (UI-01)

    2. Modify `RelayNavGraph.kt`:
    - Add import for `SessionListScreen`
    - Change the `startDestination` when configured from `"status"` to `"sessions"`
    - Add a new `composable("sessions")` route:
      ```kotlin
      composable("sessions") {
          SessionListScreen(
              onNavigateToSetup = {
                  navController.navigate("setup")
              }
          )
      }
      ```
    - Keep the existing `"status"` route as-is for backward compatibility (it's still useful as a raw debug view)
    - The "sessions" route is now the main screen users see after setup

    3. Verify the full build:
    ```bash
    ./gradlew :androidApp:assembleDebug -x lint
    ```
    The APK must assemble successfully.
  </action>
  <verify>
    <automated>cd /Users/jheyduk/prj/relay && ./gradlew :androidApp:assembleDebug -x lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MainActivity wraps content in RelayTheme (not bare MaterialTheme)
    - RelayNavGraph routes to SessionListScreen as default when configured
    - Setup screen route unchanged (still works for first-time config)
    - Debug APK assembles successfully
    - All shared tests still pass
  </acceptance_criteria>
  <done>App launches with RelayTheme, navigates to session list screen, APK assembles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of session list UI</name>
  <files>androidApp/build/outputs/apk/debug/androidApp-debug.apk</files>
  <action>
    Install the debug APK on device and visually verify all Phase 2 UI features:
    dark mode theming, session list with status chips, pull-to-refresh, FAB refresh,
    drawer navigation, command input with auto-prefix, and expandable /last responses.
  </action>
  <verify>
    <automated>cd /Users/jheyduk/prj/relay && ./gradlew :shared:allTests -x lint && ./gradlew :androidApp:assembleDebug -x lint 2>&1 | tail -5</automated>
  </verify>
  <what-built>
    Complete Phase 2 session discovery and status UI:
    - Material 3 dark mode with dynamic color theming
    - Session list with pull-to-refresh and FAB
    - Color-coded status chips (Working=Blue animated, Waiting=Orange, Ready=Green, Shell=Gray)
    - Drawer navigation with session list
    - Bottom command input with auto @kuerzel prefix
    - Expandable /last response in session cards
  </what-built>
  <how-to-verify>
    1. Install APK: `adb install -r androidApp/build/outputs/apk/debug/androidApp-debug.apk`
    2. Open app -- should show session list (or empty state if no sessions discovered yet)
    3. Verify dark mode: toggle system dark mode in Android settings, app theme should follow
    4. Pull down on the session list -- should trigger refresh (sends /ls command)
    5. Tap the refresh FAB -- should also trigger refresh
    6. Tap hamburger menu -- drawer should open with session list
    7. Select a session from drawer or list -- @kuerzel prefix should appear in command input
    8. Type `/ls` in command input -- should send without @kuerzel prefix (global command)
    9. Type `/last` in command input with session selected -- should send with @kuerzel
    10. Type plain text with session selected -- should send as @kuerzel message
  </how-to-verify>
  <acceptance_criteria>
    - Dark mode follows system setting
    - Session cards display with correct status chip colors
    - Pull-to-refresh and FAB both work
    - Drawer opens with session list
    - Command input shows @kuerzel prefix when session selected
    - Global commands (/ls) sent without prefix
  </acceptance_criteria>
  <done>User visually confirms all Phase 2 UI features work correctly on device</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/jheyduk/prj/relay && ./gradlew :shared:allTests -x lint && ./gradlew :androidApp:assembleDebug -x lint
```
All tests pass and debug APK assembles.
</verification>

<success_criteria>
- App launches with Material 3 dark mode theming (UI-01)
- Session list screen is the main destination after setup
- Koin DI resolves all dependencies: SessionRepository, SessionListViewModel
- User can see sessions, refresh via pull or FAB, send commands, navigate via drawer
- Debug APK assembled and ready for device testing
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-discovery-status/02-03-SUMMARY.md`
</output>
